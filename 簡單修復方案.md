# 🔧 最終修復方案 - 簡單版

## ✅ 我已經做了什麼

創建了新文件 `generateTestData.ts`，只生成 **500筆數據**（而不是5000筆），避免瀏覽器崩潰。

---

## 📝 你需要做的（只需2步）

### 第1步：修改 AnalyticsDashboard.tsx

打開 `src/components/AnalyticsDashboard.tsx`

**找到第58行**（約在第58行附近）：
```tsx
const { generateRealisticFiveYearData } = await import('../utils/generateRealisticData');
```

**改成**：
```tsx
const { generateTestData } = await import('../utils/generateTestData');
```

**找到第63行**（約在第63行附近）：
```tsx
const testSessions = await generateRealisticFiveYearData((generated, total) => {
```

**改成**：
```tsx
const testSessions = await generateTestData((generated, total) => {
```

### 第2步：測試

1. **保存文件** (Ctrl+S)
2. **重新整理網頁** (F5)
3. **點擊「生成數據」按鈕**

---

## 🎯 預期結果

- ✅ 生成 **500筆數據**（約180天的練習紀錄）
- ✅ 只需 **10-20秒**
- ✅ **不會崩潰**
- ✅ 可以看到完整的成長曲線

---

## 💡 完成後

如果這個版本能正常運作，之後我可以幫你增加到1000筆或更多！

現在先測試500筆版本確保穩定性 🚀
